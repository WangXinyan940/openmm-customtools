from setuptools import setup, find_packages

setup(
    name = "openmm_customtools",
    packages = ['openmm_customtools'],
    version = "0.1.0",
    author = "Xinyan Wang",
    description = "Implementations of enhanced sampling toolkits based on OpenMM CustomIntegrator and CustomBond classes."

)